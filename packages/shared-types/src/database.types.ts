/*
 * Auto-generated via `pnpm gen:types`.
 * Placeholder fallback to keep local typechecking stable before first generation.
 */

export type Json =
  | string
  | number
  | boolean
  | null
  | { [key: string]: Json | undefined }
  | Json[];

export type Database = Record<string, never>;
